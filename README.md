# Stock-Charts

A lightweight stock prediction web app that runs as a static site.

## Run locally

```bash
cd /home/runner/work/Stock-Charts/Stock-Charts
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
